server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: updategit   # Opciones: create, update, validate, none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always  # Permite cargar scripts SQL al inicio (opcional)

security:
  jwt:
    key:
      private: ${JWT_PRIVATE_KEY}
    user:
      generator: auth-service
